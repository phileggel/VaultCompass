# Dependency Audit — 2026-05-05

> Non-release audit. CVEs flagged but do not block current session.

---

## npm (`package.json`)

### 🔴 CVE — devDependencies (E2E runner only, not production)

`serialize-javascript <=7.0.4` via `@wdio/mocha-framework@9.27.1 → mocha → serialize-javascript`

- **GHSA-5c6j-r48x-rmvq** (high): RCE via `RegExp.flags` injection — fixed in serialize-javascript 7.0.3
- **GHSA-qj8w-gfj5-8c6v / CVE-2026-34043** (high): CPU exhaustion DoS — fixed in serialize-javascript 7.0.5

Context: devDependency, E2E test runner only — not in the production bundle. Upstream fix pending in mocha ([mocha#5872](https://github.com/mochajs/mocha/issues/5872)). `npm audit fix --force` would downgrade `@wdio/mocha-framework` to v6.1.17 (breaking — avoid). Wait for mocha to ship serialize-javascript 7.0.5+.

### 🔵 Minor drift — resolved in this session

All packages updated with `npm update`:

| Package                     | Was      | Now     |
| --------------------------- | -------- | ------- |
| `@biomejs/biome`            | 2.4.13   | 2.4.14  |
| `@tanstack/react-router`    | 1.168.24 | 1.169.1 |
| `@tauri-apps/api`           | 2.10.1   | 2.11.0  |
| `@tauri-apps/cli`           | 2.10.1   | 2.11.0  |
| `@tauri-apps/plugin-opener` | 2.5.3    | 2.5.4   |
| `lucide-react`              | 1.11.0   | 1.14.0  |
| `oxlint`                    | 1.61.0   | 1.62.0  |
| `react-i18next`             | 17.0.4   | 17.0.6  |
| `zustand`                   | 5.0.12   | 5.0.13  |

---

## Cargo (`src-tauri/Cargo.toml`)

### 🔴 CVE — accepted risk (pre-existing)

- `rsa 0.9.10` — **RUSTSEC-2023-0071** (medium 5.9): Marvin Attack timing sidechannel. No upstream fix. Transitive via `sqlx-mysql`; project only enables `sqlite` so the vulnerable RSA path is never reached. See `docs/todo.md`.

### 🟡 Major drift — deferred (see docs/todo.md)

- `reqwest 0.12.28` → `0.13.3`: TLS default changes from native-tls to rustls+aws-lc; breaking API removals. Requires dedicated bump with TLS feature flag review.

### 🔵 Minor drift — resolved in this session

`cargo update` applied. Key bumps: `mockall 0.13.1 → 0.14.0`.

Blocked (tauri-specta dependency): `specta rc.22 → rc.24`, `specta-typescript 0.0.9 → 0.0.11`. Tracked separately in `docs/todo.md`.

### ⚠️ Unmaintained warnings (20 — not actionable)

All transitive via `tauri → wry → tao → gtk3` on Linux (GTK3 bindings: `atk`, `gdk`, `gtk`, and sys crates, RUSTSEC-2024-0411–0420). Also `fxhash` (RUSTSEC-2025-0057). No fix available until Tauri migrates to GTK4.

---

## Rust toolchain

- Installed: `rustc 1.95.0` ✅ up to date (latest stable)

---

## Summary

|                 | Count | Status                                                                  |
| --------------- | ----- | ----------------------------------------------------------------------- |
| 🔴 CVEs         | 3     | serialize-javascript ×2 (devDep, waiting upstream); rsa (accepted risk) |
| 🟡 Major drift  | 1     | reqwest 0.12→0.13 — deferred, see todo.md                               |
| 🔵 Minor drift  | 12    | ✅ resolved via npm update + cargo update                               |
| ⚠️ Unmaintained | 20    | Not actionable (Tauri GTK3 transitive)                                  |
